<!DOCTYPE html>
<html>
	<%= render "shared/head" %>

	<body>
		<%= render "shared/header" %>
		<%= turbo_frame_tag "modal" %>
		
		<div id="flash" class="flash">
			
		</div>

		<main class="">
			<div class="container mx-auto py-2">
				<%= yield %>
			</div>
		</main>
	</body>
</html>
